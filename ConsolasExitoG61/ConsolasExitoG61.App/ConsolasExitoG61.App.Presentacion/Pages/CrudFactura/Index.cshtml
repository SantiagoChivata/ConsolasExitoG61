@page
@model ConsolasExitoG61.App.Presentacion.Pages.CrudFactura.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Facturacion</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Factura[0].Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Factura[0].Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Factura[0].ValorUnitario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Factura[0].ValorTotal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Factura) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorUnitario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorTotal)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
